---
description: 
globs: 
alwaysApply: true
---
nespreso_api/
├── data/              ─ raw & interim data helpers
├── models/            ─ PyTorch weights + TorchScript / ONNX
├── services/
│   ├── accessor/      ─ FastAPI satellite-fetch ➞ tensor
│   └── kernel/        ─ TorchServe / Triton handler
├── deployment/        ─ Docker, K8s, Helm
├── tests/             ─ unit + contract tests
├── eoas-pyutils/      ─ helper module - don't touch it
├── nespresso-ui/      ─ ui module - don't touch it
├── .cursor/rules/     ─ project rules (see below)
├── .github/workflows/ ─ CI / CD
├── nespreso_flask.py  ─ service script
├── README.md
└── requirements.txt

ignore the remainder files at the root folder
